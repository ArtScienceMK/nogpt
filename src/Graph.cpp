#include "Graph.hpp"
#include <iostream>
#include <vector>

Graph::Graph() : m_graph(0)
{
}

string Graph::findCurrentWord(const string &s)
{
    int need = (int)s.size();
    string cur = "";
    int curMatches = 0;
    string best = "";
    int bestMatches = 0;
    for (auto c : s)
    {
        if (c == ' ')
        {
            if (curMatches ==)
        }
    }
}